---
spring:
  profiles: default
  datasource:
    url: "jdbc:sqlite:/home/user/projects/bookstore7/sqlite.db"
    driver-class-name: org.sqlite.JDBC
    initialization-mode: never
    hikari:
      maximum-pool-size: 1
